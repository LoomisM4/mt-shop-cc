<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="mtshopcc.Views.Details">
  <ScrollView x:Name="View">
      <StackLayout>
          <CarouselView ItemsSource="{Binding Images}">
              <CarouselView.ItemTemplate>
                  <DataTemplate>
                      <Image Source="{Binding .}"/>
                  </DataTemplate>
              </CarouselView.ItemTemplate>
          </CarouselView>
          <Button Text="In den Warenkorb" Clicked="Button_OnClicked"/>
          <Label HorizontalOptions="Center" FontAttributes="Bold" FontSize="20" Text="{Binding Article.Name}" />
          <Label HorizontalOptions="CenterAndExpand" Text="{Binding Article.Description}" />
      </StackLayout>
  </ScrollView>
</ContentPage>
